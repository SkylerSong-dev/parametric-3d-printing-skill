# Use a Python version for which the selected CadQuery release publishes wheels.
# This package was reconstructed around Python 3.12. Re-run the full tests and
# example previews when changing Python or dependency major versions.
cadquery>=2.6.1,<3
trimesh>=4.0,<5
pyrender>=0.1.45,<0.2
Pillow>=10,<13
numpy>=1.24,<3
lxml>=4.9,<7
